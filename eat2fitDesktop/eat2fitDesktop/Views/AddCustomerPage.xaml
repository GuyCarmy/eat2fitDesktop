<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="eat2fitDesktop.AddCustomerPage">
    <ContentPage.Content>
        <StackLayout Spacing="30" Padding="30">
            <StackLayout Orientation="Horizontal">
                <Label Text="Name: "/>
                <Entry Text="{Binding Name}" WidthRequest="500"/>
            </StackLayout>
            <StackLayout Orientation="Horizontal">
                <Label Text="Password: "/>
                <Entry Text="{Binding Password}" WidthRequest="500" IsPassword="True"/>
            </StackLayout>
            <StackLayout Orientation="Horizontal">
                <Label Text="Age: "/>
                <Entry  Text="{Binding Age}" WidthRequest="500"/>
            </StackLayout>

            <Button Text="Add" Command="{Binding OnAddCustomerClickedCommand}" HorizontalOptions="Center"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>